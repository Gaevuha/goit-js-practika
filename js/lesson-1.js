//!========================================================

// Запропонуйте користувачу ввести число за допомогою prompt().
// Збережіть введене значення у змінну number.
// Перевірте, чи дорівнює змінна number числу 10:
//    Якщо так – виведіть повідомлення 'Вірно' у alert().
//    Якщо ні – виведіть 'Невірно'. 

// Підказка: prompt завжди повертає рядок (String), тому перед перевіркою  
// перетворіть значення на число за допомогою Number().


// function getNumber(number) {
//   let value = '';
//   if (Number(number) === 10) {
//       value ='Вірно';
//   } else {
//       value = 'Невірно';
//   }
//   return value;

// або
//     return (Number(number) === 10) ? 'Вірно' : 'Невірно';
// }

// const number = prompt('Введіть число 10'); // отримуємо введене значення від користувача
// console.log(getNumber(number)); // Виводимо результат у консоль
// alert(getNumber(number)); //Виводимо результат у вікно alert 

//!========================================================

// У змінній min лежить випадкове число від 0 до 59.
// Визначте, в яку чверть години потрапляє
// це число (у першу, другу, третю чи четверту).
// Виведіть в alert відповідне повідомлення, наприклад:
// "10 входить в першу чверть"

// const min = Math.floor(Math.random() * (59 - 0) + 0);

// function getNumber(min) {

//     let quarter = '';

//     if ((min >= 0 && min < 15)) {
//         quarter = "першу чверть";
//     } else if ((min >= 15 && min < 30)) {
//         quarter = "другу чверть";
//     }else if ((min >= 30 && min < 45)) {
//         quarter = "третю чверть";
//      }else if ((min >= 45 && min <= 59)) {
//         quarter = "четверту чверть";
//     } else {
//         quarter = "Введено невірне значення";
//     }
     
//     return quarter;
// }

// const min = Number(prompt('Введіть число від 0 до 59'));
// const result = getNumber(min);
// console.log(getNumber(min));
// alert(`${min} входить в ${result}`);

//!========================================================

// Змінна num може набувати 4 значення: '1', '2', '3' або '4' 
// (запитуй це значення у користувача через prompt). 
// Якщо вона має значення '1', то у змінну result запишіть 'зима',
// якщо має значення '2' - 'весна' і так далі.
// Розв'яжіть завдання через switch-case.
// Не забудьте про дефолтне значення, на випадок, якщо користувач 
// введе в prompt щось інше. В такому випадку result має набувати значення:
// "Вибачте, але ви маєте ввести значення від 1 до 4 включно".
// Значення змінної result виведіть в консоль.

// function getSeason(num) {

//     let season = '';

//     switch (num) {
//         case 1:
//             season = 'зима';
//             break;
//         case 2:
//             season = 'весна';
//             break;
//         case 3:
//             season = 'літо';
//             break;
//         case 4:
//             season = 'осінь';
//             break;
//         default:
//             season = 'Вибачте, але ви маєте ввести значення від 1 до 4 включно'
//     }
//     return season;
// }

// const num = Number(prompt('Введіть число від 1 до 4'));
// const result = getSeason(num);
// console.log(getSeason(num));
// alert(result);

//!========================================================

// Отримуйте від користувача число (кількість хвилин) через prompt
// і виводьте у консоль рядок у форматі годин та хвилин.
// Приклад: користувач вводить в prompt '70' -> в консолі відобразиться '01:10'.
// Корисне посилання для відображення годин та хвилин у потрібному форматі ('01' замість '1'): 
// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/padStart#examples


// const totalMinutes = prompt("Введіть кількість хвилин:");
    // prompt() — відкриває вікно для введення числа.
    // Користувач вводить кількість хвилин, наприклад, "70".
    // Значення, отримане з prompt(), завжди є рядком (string).

// const minutes = totalMinutes % 60;
    // Отримуємо залишок хвилин
    // 70 % 60 = 10 (10 хвилин).
    // 125 % 60 = 5 (5 хвилин).

// const hours = Math.floor(totalMinutes / 60);
    // Отримуємо години
    // Math.floor(70 / 60) = 1 (1 година).
    // Math.floor(125 / 60) = 2 (2 години).


// const formattedTime = String(hours).padStart(2, "0") + ":" + String(minutes).padStart(2, "0");
    // Форматуємо годинник і хвилини до двох цифр (наприклад, 01:05)
    // String(hours).padStart(2, "0")
    // Перетворює число hours у рядок.
    // Додає ведучі нулі, якщо число має менше 2 цифр.
    // 1 → "01", 12 → "12".
    // String(minutes).padStart(2, "0")

    // Аналогічно форматує minutes.
    // 5 → "05", 30 → "30".
    // В результаті утворюється рядок HH:MM, наприклад:

    // "01:10"
    // "02:05"
    // "00:45"

// console.log(formattedTime);
// alert(`Формат часу: ${formattedTime}`);

//!========================================================

// Напишіть код, який запитуватиме у користувача
// логін за допомогою prompt і виводить результат в консоль браузера

// Після цього додайте перевірку введеного користувачем значення:
// Якщо відвідувач вводить "Адмін",
// то prompt запитує пароль (не забудьте зберігти його у змінну для подальшої перевірки).
// Якщо нічого не ввели або натиснули Cancel,
// Вивести в alert строку "Скасовано"
// В іншому випадку вивести в alert рядок "Я вас не знаю"

// Пароль перевіряти так:
// Якщо введено пароль "Я головний",
// то вивести в alert рядок "Добрий день!",
// в іншому випадку вивести в alert рядок "Невірний пароль!"

// Функція для запиту логіна
// function getLogin() {
//     return prompt("Введіть ваш логін:");
// }

// Функція для запиту пароля
// function getPassword() {
//     return prompt("Введіть ваш пароль:");
// }

// Функція для перевірки логіна та пароля
// function checkLogin() {
//     const login = getLogin(); // Запитуємо логін

//     if (login === "Адмін") {
//         const password = getPassword(); // Якщо логін "Адмін", запитуємо пароль

//         if (password === null || password === "") {
//             alert("Скасовано"); // Якщо пароль не введено
//         } else if (password === "Я головний") {
//             alert("Добрий день!"); // Якщо пароль правильний
//         } else {
//             alert("Невірний пароль!"); // Якщо пароль неправильний
//         }
//     } else if (login === null || login === "") {
//         alert("Скасовано"); // Якщо логін не введено
//     } else {
//         alert("Я вас не знаю"); // Якщо логін не "Адмін"
//     }
// }

// Викликаємо функцію перевірки логіна та пароля
// checkLogin();

//!========================================================

// Використайте цикл while, щоб вивести в console всі числа від 0 до 20 включно.

// let count = 0;
// while (count < 20) {
//     count += 1;
//     console.log(count);
// }

//!========================================================

// Напишіть функцію getNumbers(min, max), що приймає 2 параметри - 
// мінімальне і максимальне число відповідно.
// Напишіть цикл всередині функції, який виводить у консоль
// всі числа від max до min за спаданням.
// Окрім цього, підрахуйте суму всіх парних чисел в цьому циклі
// і поверніть її з функції.

// function getNumbers(min, max) {

//     let number = 0;

//     for (let i = max; i >= min; i--) {
//         if (i % 2 === 0) {
//              number += i;
//         }
       
//         console.log(i);
//     }

//     return number;
// }


// console.log( getNumbers(2, 20));

//!========================================================

// Напишіть функцію min(a, b), яка приймає 2 числа і повертає меньше з них.
// Потрібно додати перевірку, що функція отримує саме числа, в іншому випадку
// повертати з функції рядок - 'Not a number!'.

// function min(a, b) {

//     if (isNaN(a) || isNaN(b)) {   //перевірку, що функція отримує саме числа

//         return 'Not a number!';

//     } else if (a < b) {

//         return a;

//     } else {

//         return b;
//      }
// }

// console.log(min(3, 5));

//!========================================================

// Напишіть функцію isAdult(age), яка  приймає число - вік користувача і повертає true, 
// якщо параметр age більше чи дорівнює 18.
// В іншому випадку вона запитує підтвердження через confirm 
// і повертає його результат (true/false).

// function isAdult(age) {

//     let isUserAge = '';      

//     if (isNaN(age)) {
//         return 'Введіть коректне значення';
//     }

//     if (age >= 18) {
//         isUserAge = 'Ласкаво просимо!'
//     } else {
//         isUserAge = confirm('Вам точно є 18?');
//         if (isUserAge) {
//             isUserAge = 'Ласкаво просимо!'
//         } else {
//             isUserAge = 'Вхід заборонено!'
//         }
//     }

//     return isUserAge;
// }

// const age = Number(prompt('Введіть свій вік'));
// const result = isAdult(age);
// alert(result);

//!========================================================

// Напишіть функцію fizzBuzz(num), яка приймає число і перевіряє кожне число від 1 до num:
// Якщо число ділитися  без остачі на 3 - виводить в консоль 'fizz',
// якщо ділиться  без остачі на 5 - виводить в консоль 'buzz',
// якщо ділиться  без остачі і на 3, і на 5 - виводить в консоль 'fizzbuzz'.

// function fizzBuzz(num) {

//     for (let i = 1; i <= num; i++) {
        
//         if (i % 3 === 0 && i % 5 === 0) {
//             console.log('fizzbuzz');
//         } else if (i % 3 === 0) {
//             console.log('fizz');
//         } else if (i % 5 === 0) {
//             console.log('buzz');
//         } else {
//             console.log(i);
//         }
//     }
// }

// fizzBuzz(15);





